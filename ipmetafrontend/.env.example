# Base URL of the ipmetabackend API (including the /api prefix).
# Used both server-side (page data fetching) and client-side (repo pagination,
# AI guide chat), so it must be reachable from the browser too.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
